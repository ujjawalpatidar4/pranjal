# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Firebase Hosting CI for Merge

on:
  push:
    branches:
      - master  # Replace with your main branch if different

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '16'  # Adjust to your required Node.js version

    - name: Install dependencies
      run: |
        npm install --legacy-peer-deps
      env:
        NODE_ENV: development  # Ensure devDependencies are installed

    - name: Build project
      run: npm run build

    - name: Deploy to Firebase Hosting
      uses: firebase-tools/action-hosting-deploy@v0
      with:
        repoToken: ${{ secrets.GITHUB_TOKEN }}
        firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_AGRAWALTRANSPORT_5863A }}  # Your service account secret
        channelId: live  # Channel ID for production hosting
        projectId: agrawaltransport-5863a  # Your Firebase project ID  # Your Firebase project ID
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}  # Your Firebase token secret
