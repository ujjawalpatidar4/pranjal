# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Firebase Hosting CI for Merge

on:
  push:
    branches:
      - master  # or the relevant branch

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '16'  # Adjust to your Node.js version

    - name: Install dependencies
      run: |
        npm install --legacy-peer-deps  # Ensures all dependencies are installed
      env:
        NODE_ENV: development  # Ensures devDependencies are installed

    - name: Build project
      run: npm run build

    - name: Deploy to Firebase Hosting
      run: firebase deploy --only hosting --token ${{ secrets.FIREBASE_TOKEN }}

      with:
        repoToken: ${{ secrets.GITHUB_TOKEN }}
        firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_AGRAWALTRANSPORT_5863A }}
        channelId: live
        projectId: agrawaltransport-5863a
